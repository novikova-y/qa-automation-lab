{
  "name": "qa-automation-lab",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "clean:allure": "rimraf allure-results allure-report cypress/screenshots",
    "cy:run:allure": "npm run clean:allure && cypress run --env allure=true && allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.2.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-commandline": "^2.34.1",
    "cypress": "^14.5.3",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "rimraf": "^6.0.1"
  }
}
